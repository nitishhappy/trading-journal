{
  "name": "trade-journal-smoke-tests",
  "private": true,
  "description": "Standalone smoke test for the Trade Journal frontend — deliberately separate from the Cloud Functions package.json in the project root.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
